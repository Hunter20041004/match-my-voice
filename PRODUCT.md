# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要使用者：從 GitHub 把 `match-my-voice` 裝進 Claude Code／Codex 的人（使用者 2026-09-22 確認：「給從 GitHub 裝技能的所有人」，不是只有作者本人）。
他們平常在終端機裡跟 Agent 對話，偶爾另開這個本機管理頁面：看自己有哪幾個角色、切換目前角色、直接改口吻檔、撤銷或改字 Agent 學錯的規則。
第一次打開時多半什麼都沒有（沒有角色、口吻檔是空的、沒學到任何東西），介面必須能把人帶到「回對話請 Agent 收集樣本」這一步。

次要：作者本人（產品決策者，零程式背景），把它當自己的日常工具。

## Product Purpose

讓 Agent 用使用者本人的口吻寫東西。三個核心功能：找樣本、多角色、從使用者的修改學（反哺）。
管理頁面是這三件事的「檔案櫃」：口吻檔與學到的規則人看得懂、改得了、撤得掉。
成功的樣子：使用者改完口吻檔或撤掉一條學錯的規則，下一次 Agent 寫出來的東西就變了，中間不需要任何其他步驤。

## Positioning

- 不掃整個帳號，只讀使用者勾選的樣本；口吻檔是一份看得到、改得了的 Markdown。
- **從使用者的修改學**，不是只讀既有文件（ChatGPT Work「Writing style」讀既有文件）。中文市場沒有產品做這件事。
- 資料全在本機 `~/.config/match-my-voice/`，工具本身不呼叫任何 AI、不需要金鑰、沒有伺服器在我們這邊。
- 這是「組合與定位」的價值，不是技術突破（`docs/research/`）。

## Operating Context

- 頁面由 `node ui/serve.js` 在本機起，網址是 `http://127.0.0.1:<隨機埠>`，只有本人看得到。
- 通常跟終端機並排開；是「看一眼、改兩行、撤一條」的工具，不是長時間停留的工作台。
- 資料來源是磁碟上的檔案：`config.json`、`profiles/<id>/{persona.json,VOICE.md,learned.md}`。Agent 在對話裡寫進去，頁面讀出來；頁面改了，Agent 下次讀就生效。
- 頁面跟對話彼此不知道對方在哪（已知的洞）。

## Capabilities and Constraints

已有：列角色、新增（名稱＋self／role）、設為目前、刪除、讀寫 `VOICE.md`、列學到的規則、撤銷／恢復、改規則文字。
2026-09-22 加：改角色顯示名稱（遷移來的角色叫「default」，之前改不了）。

硬限制：
- `ui/serve.js` 與測試只用 Node 內建模組；`ui/index.html` 單檔、Tailwind CDN、CSS 變數當設計代幣。沒有 build 步驟。
- 介面文字繁體中文（技能本體是英文，不換）。
- 任何真人姓名、樣本、口吻檔不進版控。

術語：**角色（persona）** = 一份口吻；`self` 是自己，`role` 是代表的組織或身分。**目前角色（active）** = Agent 預設用的那個。**學到的規則（learned）** = 反哺後寫進 `learned.md` 的一行一條規則，可撤銷（revoked）。

未決：頁面如何提醒使用者「把改好的草稿貼回來」；頁面與對話的連結方式。

## Brand Commitments

名稱 `match-my-voice`。視覺方向已核可（2026-09-21，使用者三輪回饋後定案）：紫羅蘭主色 Radix Violet 9 `#6E56CF`、淡紫底 `#F7F6FB`，材質參考 Clerk／Linear——髮絲線取代陰影、圓角 8、等寬字當小標籤、極淡格線底。**不重問方向。**

## Evidence on Hand

- `README.md`：完整使用說明（繁中）。
- `docs/research/2026-09-21-口吻提煉與編輯學習方法.md`：方法論研究，結論是效果尚未校準。
- 沒有使用者見證、沒有數據、沒有 logo；不得虛構。

## Product Principles

1. 你看到的就是 Agent 讀到的：頁面不做任何隱藏加工，檔案內容原樣呈現、原樣寫回。
2. 可撤銷優先於防呆：學錯的規則一鍵撤掉、可恢復；刪角色這種不可逆的才擋一下。
3. 空狀態要指路：第一次打開什麼都沒有，每個空區塊都告訴人下一步在對話裡怎麼說。
4. 這是工具不是儀表板：快、安靜、不搶注意力；品牌只活在細節裡。
5. 不宣稱沒驗證過的效果。
